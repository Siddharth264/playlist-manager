export type TypePlayList = {
    id: string;
    name: string;
    thumbnail: string;
    videoCount: number;
    playListId: string;
}

export type TypeVideo = {
    id: string;
    title: string;
    description: string;
    duration: string;
    productsCount: number;
    thumbnail: string;
}