export type TypePlayList = {
    id: number;
    name: string;
    thumbnail: string;
    videoCount: number;
}

export type TypeVideo = {
    id: number;
    title: string;
    duration: string;
    productsCount: number;
    thumbnail: string;
}